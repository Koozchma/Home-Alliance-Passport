/**
 * Sample Test File
 *
 * This is a placeholder test to verify that the test runner is working.
 * Replace or expand this file as you build out functionality.
 */

describe("Sample Test", () => {
  it("should pass successfully", () => {
    expect(true).toBe(true);
  });

  it("adds numbers correctly", () => {
    const sum = 2 + 3;
    expect(sum).toBe(5);
  });
});
